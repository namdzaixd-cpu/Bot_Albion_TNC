See [CLAUDE.md](CLAUDE.md) for working instructions on this repo (workflow rules, code style,
project structure). Same instructions apply regardless of which AI agent is being used.

# LUẬT THÉP CỦA TNC (QUAN TRỌNG NHẤT):
1. LUÔN LUÔN đọc các file `.md` (đặc biệt là `instruction.md` và `CLAUDE.md`) trước khi làm việc.
2. Khi User yêu cầu tính năng mới hoặc fix bug, PHẢI bàn thiết kế, đưa ra giải pháp trước.
3. KHÔNG ĐƯỢC PHÉP tự ý viết, sửa code hay chạy lệnh Git khi CHƯA có lệnh.
4. CHỈ ĐƯỢC BẮT ĐẦU CODE khi User gõ RÕ RÀNG: "chốt", "ok làm đi", "làm đi", "chốt code đi".
5. Nếu chưa thấy từ khóa chốt, bắt buộc phải hỏi lại "Bro chốt chưa?".
6. Khi làm xong, cung cấp lại danh sách các lệnh Git cần chạy, hoặc tự động chạy nếu được phép.
